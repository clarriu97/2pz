# GitHub (repo automation only — not used at runtime)
GITHUB_TOKEN=

# OpenAI — required ONLY for (a) regenerating analyst notes offline and (b) the live chat endpoint.
# The product runs fully without it: notes are committed to data/processed/.
OPENAI_API_KEY=

# Cloudflare (deploy)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
